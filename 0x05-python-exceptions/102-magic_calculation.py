def magic_calculation(a, b):
    result = 0


    for i in range(1, 4):
        try:
            if i > a:
                raise Exception('Too far')
            except Exception:
                continue:


                    result += (a ** b) / i


                    result += b + a
                    return result;
